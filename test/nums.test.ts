import {describe, test, expect} from "@jest/globals";

let {twoSum} = require("../src/nums");

describe("if `arr` contains `searchElement`, return true, or return false", () => {
    test("加法测试", () => {
        expect(1 + 1).toEqual(2)
    })
})
